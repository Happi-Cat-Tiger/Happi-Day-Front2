'use client';
import AllPage from './all/page';

const BoardPage = () => {
  // useEffect(() => {
  //   throw new Error('테스트용 에러 발생!');
  // }, []);

  return <AllPage />;
};

export default BoardPage;
